import styled from 'styled-components';

const Wrapper = styled.div`
	width: 100%;
	height: 62px;
	background-color: black;
`;

function Header() {
	return <Wrapper>:Frog:</Wrapper>;
}

export default Header;
